<!DOCTYPE HTML>
<html>

<head>
    <title>SHOP PROJECT</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/normalize.css@8.0.1/normalize.css" />
    <link rel="stylesheet" href="/<%= process.env.ADMIN_PATH %>/css/style.css" />
</head>

<body>
  <header class="header">
    <div class="logo"></div>
    <% if (!location.includes("/login")) { %>
      <a  class='header-links' href="<%= `/${process.env.ADMIN_PATH}/new-product` %>">add product</a>
    <% } %>
    <a class="header-links" href="<%= `/${process.env.ADMIN_PATH}/auth/logout` %>">logout</a>
  </header>

  <div class="container">
    <%- body %>
  </div>
</body>

</html>